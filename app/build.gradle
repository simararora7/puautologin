apply plugin: 'com.android.application'

android {
    signingConfigs {
        config {
            keyAlias 'puautologin'
            keyPassword 'awesum77'
            storeFile file('D:/AndroidstudioProjects/KeyStores/puautologin.jks')
            storePassword 'awesum77'
        }
    }
    compileSdkVersion 21
    buildToolsVersion "21.1.2"
    defaultConfig {
        applicationId "simararora.puautologin"
        minSdkVersion 14
        targetSdkVersion 21
        versionCode 1
        versionName "1.0"
    }
    buildTypes {
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android.txt'), 'proguard-rules.pro'
            signingConfig signingConfigs.config
        }
    }
}

dependencies {
    compile fileTree(include: ['*.jar'], dir: 'libs')
    compile 'com.android.support:appcompat-v7:21.0.3'
    compile 'com.github.fengdai:alertdialogpro-theme-material:0.2.1'
}
